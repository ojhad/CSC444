<% if current_user.is_admin? %>
<ol>
  <% @users.each do |user| %>
      <li><%= link_to "#{user.first_name} #{user.last_name}", impersonate_user_path(user), method: :post %></li>
  <% end %>
</ol>
<% else %>
    <h1>This page is only available for Admins</h1>
<% end %>