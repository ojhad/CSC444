<div class="container charges">
  <h4> <%=t :chargeHistory%></h4>
  <br>
  <p> <%=t :chargesDesc%></p><hr>
  <div class="col-md-6">
    <% if !@charges.blank?%>
        <% @charges.each do |c| %>
            <div class="row">
              <table style="width:100%" >
                <tr>
                  <th>ID</th>
                  <th><%=t :amount%> (CAD)</th>
                  <th>Date</th>
                </tr>
                <tr>
                  <td><%=c.id%></td>
                  <td>$<%=c.amount%></td>
                  <td><%=c.created_at.strftime("%B %d, %Y")%></td>
                </tr>
              </table>
            </div>
            <hr>
        <%end%>
    <%else%>
        <p><%=t :noCharges%></p>
    <%end%>
  </div>
</div>